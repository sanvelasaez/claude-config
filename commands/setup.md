Actualiza la configuración global de Claude Code ejecutando:

```bash
npx --yes github:sanvelasaez/claude-config
```

El instalador se encarga de todo: descarga la última versión, copia los archivos a ~/.claude/ y configura el MCP centinel en ~/.claude.json automáticamente.

Cuando termine, informa brevemente al usuario del resultado: cuántos archivos se actualizaron y si el hook funciona.
