# Dependencias Python para la configuración global de Claude Code
# Instalar con: pip install -r requirements.txt

# MCP SDK — requerido por mcps/centinel-server.py
# Proporciona el protocolo de comunicación con Claude Code para el MCP server de seguridad
mcp>=1.0.0
